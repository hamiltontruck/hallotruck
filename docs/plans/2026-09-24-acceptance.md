# Acceptance criteria

The scope is complete only when truck cards no longer crowd each other, truck capacity deterministically controls locked Total Weight, Orders/tracking match the approved Customer portal behavior, the database rejects a second same-day driver assignment under concurrent and privileged paths, and all CI/mobile smoke gates are green.
