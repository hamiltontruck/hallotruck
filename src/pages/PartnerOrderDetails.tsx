import { useEffect, useState } from "react";
import { Link, useParams, useSearchParams } from "react-router-dom";
import { getPartnerOrder, submitPartnerOrder, type PartnerOrder, type PartnerOrderHistory } from "../services/partner-order.service";

export function PartnerOrderDetails(){const {orderId}=useParams();const[params]=useSearchParams();const[order,setOrder]=useState<PartnerOrder|null>(null);const[history,setHistory]=useState<PartnerOrderHistory[]>([]);const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function load(){if(!orderId)return;try{const result=await getPartnerOrder(orderId);setOrder(result.order);setHistory(result.history);}catch(reason){setError(reason instanceof Error?reason.message:"Partner order could not be loaded.");}}useEffect(()=>{void load();},[orderId]);async function submit(){if(!order)return;setBusy(true);setError("");try{await submitPartnerOrder(order.id);await load();}catch(reason){setError(reason instanceof Error?reason.message:"Partner order could not be submitted.");}finally{setBusy(false);}}const organization=params.get("organization");const back=organization?`/partner/orders?organization=${encodeURIComponent(organization)}`:"/partner/orders";return <main className="min-h-screen overflow-x-hidden bg-[#f5f3ed] text-asphalt"><header className="bg-asphalt px-4 py-7 text-white sm:px-7"><div className="mx-auto flex max-w-5xl flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="font-mono text-[10px] text-amber">{order?.reference??"PARTNER ORDER"}</p><h1 className="mt-2 font-display text-3xl font-bold">Order details</h1><p className="mt-2 text-sm text-white/55">Tenant-isolated order record and immutable lifecycle history.</p></div><Link to={back} className="min-h-11 border border-white/20 px-4 py-3 text-center text-xs font-semibold">Back to orders</Link></div></header><section className="mx-auto max-w-5xl space-y-5 px-4 py-5 sm:px-7">{error&&<p className="border border-route/30 bg-route/5 p-4 text-sm text-route">{error}</p>}{!order&&!error&&<p className="bg-white p-10 text-center text-sm text-steel">Loading order…</p>}{order&&<><div className="grid gap-4 md:grid-cols-2"><Card title="Route"><p>{order.pickup_location.city}, {order.pickup_location.country}</p><p className="text-steel">{order.pickup_location.address}</p><p className="my-3 text-amber-dim">↓</p><p>{order.dropoff_location.city}, {order.dropoff_location.country}</p><p className="text-steel">{order.dropoff_location.address}</p></Card><Card title="Cargo & vehicle"><p className="font-semibold">{order.cargo.description}</p><p className="mt-2 text-sm text-steel">{order.cargo.weight_tons} tons · {order.cargo.quantity} units</p><p className="mt-2 text-sm text-steel">{order.vehicle_requirements.truck_type} · {order.vehicle_requirements.required_capacity_tons} tons required</p></Card><Card title="Schedule"><p>Pickup {order.schedule.pickup_date} {order.schedule.pickup_time}</p><p className="mt-2 text-sm text-steel">Priority: {order.schedule.priority}</p></Card><Card title="Contacts"><p>{order.pickup_contact.name} · {order.pickup_contact.phone}</p><p className="mt-2">{order.delivery_contact.name} · {order.delivery_contact.phone}</p></Card></div><div className="border border-asphalt/10 bg-white p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase text-steel">Current state</p><p className="mt-1 font-display text-2xl font-bold capitalize">{order.status.replaceAll("_"," ")}</p></div>{order.status==="draft"&&<button onClick={()=>void submit()} disabled={busy} className="min-h-12 bg-asphalt px-5 text-sm font-bold text-white disabled:opacity-40">{busy?"Submitting…":"Submit for HALLO review"}</button>}</div></div><Card title="Lifecycle history">{history.map((event)=><div key={event.id} className="border-b border-asphalt/10 py-3 last:border-0"><p className="font-semibold capitalize">{event.to_status.replaceAll("_"," ")}</p><p className="mt-1 text-xs text-steel">{event.reason} · {new Date(event.created_at).toLocaleString()}</p></div>)}</Card></>}</section></main>}
function Card({title,children}:{title:string;children:React.ReactNode}){return <section className="min-w-0 border border-asphalt/10 bg-white p-5"><h2 className="mb-4 font-display text-xl font-bold">{title}</h2>{children}</section>}
